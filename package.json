{
  "name": "faceup",
  "private": true,
  "version": "1.131.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --config vite.worker.config.js && node scripts/copy-static.mjs"
  },
  "devDependencies": {
    "@openai/sites-vite-plugin": "0.2.0",
    "vite": "^8.0.0"
  }
}
